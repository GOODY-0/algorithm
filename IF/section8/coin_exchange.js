const solution = (coninTypes, amount) => {
	let answer;
	let sum = 0;
	const DFS = (depth) => {
		if (sum === amount) return;
		else {
		}
	};
	console.log(answer);
	return answer;
};

solution([1, 2, 5], 15); // 3
// 1, 2, 5 로 15를 만들 수 있는 모든 경우의 수 ( 각 숫자는 무한정 재활용 가능) 중에서 가장 수가 적은 것..
// 1 * 15
// 2 * 7 + 1 * 1
//
