const solution = (tests) => {
    const students = [];
    let cnt = 0;

    for(let i = 0; i < tests.length; i++) {
        for(let j = 0; j < tests[i].length; j++) {
            tests[i][j] tests[i][j] 보다 j가 크면 cnt++
        }
    }
}
const tests = [[3,4,1,2], [4,3,2,1], [3,1,4,2]];
solution(tests); 