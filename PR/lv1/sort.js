// class quickSort {
//     constructor(array) {
//         this.array = array,
//         this.pivot = this.array[array.length - 1],
//         this.leftPointer = this.array[0],
//         this.rightPointer = this.array[this.array.length-2]
//     }

//     distribute(lp, rp, pivot, array) {

//     }
// }



// const distribute = (lp, rp, pivot, array) => {

    
// }

// const sort1 = new quickSort([0,5,2,1,6,3]);

const arr = [0,5,2,1,6,3];
let pivot = arr[arr.length-1];

let rpIndex = arr.length-2;
let rightPointer = arr[rpIndex];

for(i = 0; i < arr.length; i++) {
    let leftPointer = arr[i];
    
}